{
  "name": "digigames",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "install:api": "cd api && npm install",
    "start": "cd api && npm start",
    "migrate": "cd api && npm run migrate",
    "migrate:status": "cd api && npm run migrate:status",
    "postinstall": "cd api && npm install"
  }
}
